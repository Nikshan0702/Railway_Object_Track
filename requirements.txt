ultralytics>=8.2.0
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0
onnx>=1.15.0
# onnxruntime is optional and may not be available for Python 3.14+ on macOS arm64; install separately if supported.
tqdm>=4.66.0
